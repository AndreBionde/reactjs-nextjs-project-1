describe('<Home />', () => {
  it('teste one', () => {
    expect(1).toBe(1);
  });

  it('teste two', () => {
    expect(1).toBe(1);
  });

  it('teste three', () => {
    expect(1).toBe(1);
  });
});